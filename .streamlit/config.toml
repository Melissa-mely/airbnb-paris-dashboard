[theme]
# Bleu aligné sur le dégradé des graphiques (COLOR_SCALE dans data_utils.py),
# pour que les widgets (tags de filtre, curseur, boutons) ne détonnent plus
# avec le rouge par défaut de Streamlit.
primaryColor = "#2a78d6"
