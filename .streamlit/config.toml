[theme]
# Bleu-gris discret pour les widgets (tags de filtre, curseur, boutons) :
# assez neutre pour ne pas concurrencer visuellement les graphiques (dégradé
# bleu plus saturé, voir COLOR_SCALE dans data_utils.py), tout en remplaçant
# le rouge par défaut de Streamlit.
primaryColor = "#94a3b8"
